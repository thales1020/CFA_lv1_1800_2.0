// Zustand store for exam state management
import { create } from 'zustand';

interface ExamStore {
  // Add store state and actions here
}

export const useExamStore = create<ExamStore>((set) => ({
  // Initial state
}));
